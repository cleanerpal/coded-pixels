/**
 * Publish site Callable (B3-001).
 * Implementation lives in `callables/publishSite.ts`; this module preserves the historical path.
 */
export {
  handlePublishSite,
  publishSitePayloadSchema,
  type PublishSitePayload,
  type PublishSiteResult,
} from '../callables/publishSite';
