/**
 * Marketing config types — canonical source is @codedpixels/shared-types (B0 monorepo).
 * This module preserves the pre-monorepo `types/index.ts` import path.
 */
export type {
  BillingCycle,
  ConfigSnapshot,
  ConfigState,
  CustomTemplateBilling,
  FeatureId,
  LineItem,
  PackageId,
} from '@codedpixels/shared-types';
