rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // INF-002: replace with full M3 rules from docs/specs/firestore-rules-spec.md §11
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
